clone_depth: 1

init:
- SET PATH=%PATH%;%PYTHON%;%PYTHON%\Scripts
- refreshenv

environment:
  PYTHON: C:\Python37-x64
  PYTHON_VERSION: 3.7
  PYTHON_ARCH: 64
  KIRA_VARIABLE: Queen

configuration:
- Debug
- Release

build_script:
- echo %KIRA_VARIABLE%
- python --version

for:
-
  matrix:
    only:
    - configuration: Release
  environment:
    KIRA_VARIABLE: Goddess